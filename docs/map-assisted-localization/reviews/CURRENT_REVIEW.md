# MarketScanner current code-review entry point

> Document status: **当前有效 / current and authoritative**. Last reconciled: 2026-08-09.
> Current external review input: `MarketScanner_Mobile_Only_V1R5_Second_Independent_Full_Code_Review_NO_GO.md`.
> Current implementation input: `MarketScanner_Mobile_Only_V1_Release_Candidate_Blocker_Closeout_Prompt_V2.md`.
> Current ESL blocker-closeout input: `MarketScanner_ESL_Barcode_Capture_and_Shelf_Confirmation_Fix_Prompt.md`.
> Current MapCase02 input: `MarketScanner_MapCase02_Standard_Workbook_Format_Geometry_Fix_Prompt.md`.
> Historical predecessor input: `MarketScanner_P7R4_Review_P7R5_and_Mobile_Only_Master_Prompt.md` and `MarketScanner_P7R1_Agent_Repair_Prompts.md`.
> Production-readiness frozen baseline: `repair-v2-w2r-safety-closeout@cf1b62c949f3574e1804808537e38c8ff643549c`.
> P7R1 review baseline: `repair-v2-p7r0-independent-baseline@79c86b120fc47c28bd350e86abc9b0354d7e838d`.
> P7R3 cloud base at task start: `repair-v2-p7r3-recovery-episode-closeout@998c175e40562fffd85fe45579a358c485a65b30`.
> P7R5 base branch: `repair-v2-p7r4-recovery-confidence-closeout@e2b1cf4142b5a3bc353909af77158199bbf09e5f` (P7R4 production implementation `981ff4e208f74c8d4dd451d32df88233089e3201`).
> P7R5 governance HEAD used as the P7R6 base: `repair-v2-p7r5-recovery-terminal-closeout@970d03fbf18f8c9274f6dfb7ee0b8a79ee193c3f` (P7R5 implementation SHA `fc5618c84280cb02cfda4a8ead03f71401d4b55c`).
> P7R6 governance HEAD used as the P7R6A base: `repair-v2-p7r6-recovery-evidence-integrity-closeout@4bce394bdfb7f6c7c2373f314814a3e3260356fe` (P7R6 implementation SHA `a315ff6e5c0ca639f12c639aaf81be6738195e2f`).
> P7R6A governance HEAD used as the P7R6B base: `repair-v2-p7r6a-recovery-persisted-parser-closeout@f4524d958913e927a33a7295f45ccf7b3a98d42a` (P7R6A implementation SHA `01a42a40e9671c709c4e0f9e1f85839a48dd4a83`).
> P7R6B governance HEAD used as the P7R6C base: `repair-v2-p7r6b-strict-json-pending-queue-closeout@fd3fb4a84bd8da3770a81d455c2cc428f7479567` (P7R6B implementation SHA `fd3fb4a84bd8da3770a81d455c2cc428f7479567`).
> Current independent fix branch: `codex/fix-mobile-prior-map-id-canonicalization`, based on `f32ef7258453c6f347ed0460a4e87f0ebc035c0b`; the frozen candidate branch is not modified. The preceding RC implementation is I11 `37e6ed8c4afa00202693cd56919aea78fd4c7af5`; governance G11 `7eef33e` binds descriptor `implementation_sha` to I11. Run `31307753672@8f0e730d92773eea2ab58f56742d901ac02eead4` passed the complete host/RSS and all non-Apple gates but failed iphoneos cold configure; I11 closed that resource-tool discovery blocker locally. The current XLSX fix adds host and unsigned iphoneos evidence but still requires a new exact-SHA run. Mobile V1 follows Route A: Fast reduced graph, at most one Full existing-graph optimization, then `RESCAN_SESSION`; True sensor Deep remains outside V1.
> Mobile-Only V1 modules: docs/mobile-only/.

The authoritative release judgment remains [Production Readiness Review](PRODUCTION_READINESS_REVIEW.md): **NO-GO / NOT PRODUCTION READY**.

The MapCase02 review increment is complete. All P0/P1 findings in the formal workbook/import/compiler/package-integrity/library-install path were fixed in the current diff. The 2026-08-09 device failure exposed that the compiler emitted `Piaseczno-*` while MobileMapLibrary accepted lowercase paths only. The repaired identity is `piaseczno-5ddfac7dc439`; frozen evidence is source `1838`, active `1630`, shelf `1301`, fixed `329`, presentation `208`, canonical `5ddfac7dc439afc45abdcf800b799c05d53704895b620d161ef08a442c55b2db`, Swift package `8d3564ce68aadb087a2820a02b4747d15ea1f4d22b14e8776f913d33775b1b84`, PC package `41332d093e652ec2de94f0f86b8f15107cd6f67f3b2e5ddec1c0685ab4d7d3be`, and PC preview `d0c02be63dff3ab002dcf931ce7d0c5149152b78bea139fb1b0a2d86be196a18`; Swift/Python now assert these exact goldens rather than only self-consistency. Final review also closed the production-boundary P1 group: legacy uppercase v2 acceptance is opt-in diagnostic-only, while ordinary iOS/PC validators, the old iOS wizard, offline localization and MobileMapLibrary reject it; Swift validation reports bind `node_count`/`edge_count` to the road graph so the exact phone-generated package passes the Python production validator; both validators strictly reject bool/integral-float manifest/report counts plus non-array warnings/malformed rows; and legacy v1 element statistics/visible/hidden counts are re-derived with the same strict rules. Four real workbooks pass Swift compile → integrity → install → register → list → exact read. The allowed statement is `MAPCASE02 / STANDARD SUPERMARKET XLSX FORMAT PASS`; this does not close J-04 or change the overall **REJECTED / NO-GO / developer smoke only** judgment. Remaining P2 and deferred qualification are tracked in [`../MAPCASE02_TODO.md`](../MAPCASE02_TODO.md).

The first MapCase02 validation HEAD, `770d94b078a0dd94653b9d6b33576890f88f7296`, produced exact-SHA run [`31299358502`](https://github.com/mcjiansheng/MarketScanner/actions/runs/31299358502): 7 of 8 required jobs passed. The sole failure was the macOS/iOS 200k tag-evidence peak RSS, `812,892,160` bytes versus the frozen `768 MiB` limit; the functional result, CPU/wall budget, P0 safety, build identity, ABI, Linux/Windows native and Python/API/Web jobs passed. I7 `cbba284ad1b0694f5302ec3abbb9a446d9d3a970` removes redundant observation/view/tracking String retention from the 200k burst index while preserving key lookup and exact-field/duplicate/unconsumed rejection. Its local production-scale peak is `629,735,424` bytes and the default host suite is PASS at `493,338,624` bytes. Independent re-review of I7 is `P0=0 / P1=0`. This is local evidence only: no freeze tag may be created until the replacement final exact SHA passes every required job.

Replacement run [`31301693439@a61920b995d8c95d10e5353bfd593a21a941c1c9`](https://github.com/mcjiansheng/MarketScanner/actions/runs/31301693439) proves that I7 closes the RSS gate on the hosted runner (`790,839,296 < 805,306,368` bytes). It nevertheless remained 7/8 because a later Map quarantine race fixture used a fixed `asyncAfter(15 ms)` and exited 94 when scheduler timing no longer aligned with the intended open/read window; the Apple build steps were not reached. I8 `0a3606ecd4e06086ea95c0ab99d92f4e80fcc2dc` replaces that arbitrary delay with default-nil host synchronization after no-follow descriptor open and identity binding but before read. Production still reads the original FD and performs the same post-read pathname/inode/root checks. Both original cases pass ten repeated rounds each, the default host suite passes, and independent review is `P0=0 / P1=0`. The 128 MiB fixture reduction and more specific rejection-category assertion are P2 TODO only. A new final exact-SHA all-green run is still required before freeze.

I8 validation run [`31303500822@9478aa5981c664abb98760e24aaba2a4ec537e12`](https://github.com/mcjiansheng/MarketScanner/actions/runs/31303500822) again proves the RSS gate (`793,296,896 < 805,306,368` bytes) and closes exit 94, but remains 7/8 because a still-later tombstone source-replacement fixture exited 91 during its two-step `RENAME_EXCL` instead of reaching boundary 79. I9 `5c576dc0818f0908ef05ac1333b189aa9743d211` replaces that test-only two-step window with same-parent `RENAME_SWAP`: after the tombstone is durable, source and the byte-identical clone exchange atomically, leaving the original inode at `tombstone-original-*`. Swap failure still exits 91; only a successful swap reaches 79, and restart must still return 19 while preserving tombstone/source/original. The exact boundary passes 50 repeated local rounds plus the default host suite; independent review is `P0=0 / P1=0`. Explicit pre/post inode-direction and payload-byte assertions are deferred P2. A new final exact-SHA all-green run is still required before freeze.

I9 validation run [`31305157950@b22bd8878fcf001b38cca275b8eddcc6e48974e8`](https://github.com/mcjiansheng/MarketScanner/actions/runs/31305157950) again remains 7/8. It passes the RSS gate at `792,576,000` bytes and passes the I8/I9 fixtures, then fails because the mode-restore replacement test observes `0555` at the source path instead of the prepared clone's `0755`: its two-step rename could fail before mutation while the outer worker still returned the expected generic 19. I10 `2b111d351173b80575d37229ad55c13b42d8c3f9` / G10 `f7cad97` replaces that and three sibling test windows with same-volume `RENAME_SWAP`, binds source/displaced modes, inodes and payload bytes, and reserves exit 95 for map-root mutation failure. The final source passes 204 focused executions, the default host suite at `493,305,856` bytes, and the full Python wrapper in 973.769 s with 200k+200k tag peak RSS `688,111,616` bytes. Two read-only reviews conclude `P0=0 / P1=0`. The remaining lock rename/open window is P2 in `MAPCASE02_TODO.md`; a new final exact-SHA all-green run is still required before freeze.

I10 validation run [`31307753672@8f0e730d92773eea2ab58f56742d901ac02eead4`](https://github.com/mcjiansheng/MarketScanner/actions/runs/31307753672) passes all seven non-Apple jobs and, inside the Apple job, the complete platform-independent host contract, cold SwiftPM resolution, Xcode metadata, and the 200k tag-evidence RSS gate at `794,099,712 < 805,306,368` bytes. The sole failure is the cold-cache iphoneos dependency build: host prebuild writes `rtabmap-res_tool` under `rtabmap/prebuild/bin`, while the cross-build's implicit lookup searches a different sibling path. I11 `37e6ed8c4afa00202693cd56919aea78fd4c7af5` / G11 `7eef33e` validates the executable and passes its exact path through `RTABMAP_RES_TOOL`. A fresh host prebuild, fresh iphoneos CMake configure, focused 22/22, Map Studio 109/109, and independent review all pass with `P0=0 / P1=0`. Device/simulator clean link and a replacement exact-SHA 8/8 remain required; no freeze tag exists.

The current RC code addresses the second V1R5 review except for the explicitly retained RC-B19/J-04 component-identity blocker. V4's macOS/iOS failure exposed repeated default E2E execution in crash workers and a fixed 350 ms timer wait; after those test-scheduling fixes, the now-completing host matrix exposed a real Result publication P1: an identity-mismatched removal tombstone could be restored to the canonical intent basename and trusted on a later recovery. I5 permanently quarantines the replacement inode under `.result-publish-intent-conflict-*`, so future recovery remains fail-closed. Its historical exact long host method passed in 943.159 s; the I10 final source and remote run pass the complete wrapper. The current MapCase02 worktree has the previous 217 non-long PriorMap PASS plus the new formal PC golden test, Map Studio 109/109, Swift/PC exact-golden formal suite and four-workbook library smoke. Independent read-only reviews found the lowercase, frozen-SHA, legacy-validator and cross-end validation-report P1s; all are fixed in the current diff. Final focused re-review concludes `P0=0 / P1=0`. Exact-SHA all-green CI, Replay/FAR, Apple clean Release builds/Device Lab and Sam field re-test remain open. The permitted status is **REJECTED / NO-GO / developer smoke only**.

The ESL blocker-closeout increment replaces the old one-frame barcode action with an ARFrame-only, camera-preview capture state machine: real Vision ROI, 8 Hz one-in-flight detection, 24 Hz bounded preview, two-frame candidate lock, three-frame minimum/four-frame target and a two-second minimum-frame fallback. Every Vision request has an independent one-second ARFrame deadline. The executor is bounded to two worker lanes: a timed-out lane is best-effort cancelled and quarantined so a fresh request can start on the alternate lane; exhaustion of both lanes terminates only the ESL UX without creating a third worker or unbounded backlog. It never creates a second camera and never pauses ARSession, RTAB-Map, the continuous database, clock/pose/node production or prior-map localization. Only three independently reliable frames agreeing on the exact shelf segment and side authorize the dedicated shelf-confirmation sheet. Durable observations and complete bursts are cross-bound by observation/burst/frame/payload/symbology on iOS finalization and the PC; localized tag v2 preserves algorithm evidence separately from `USER_CONFIRMED` / `USER_OVERRIDDEN` evidence. PC input manifest v3 binds the burst sidecar; only localized tag v2 uses verified-burst exact-node authority, while legacy tag v1 keeps its versioned explicit-node/timestamp path. A consistent optimized association stays approved with `NO_CONFLICT`; conflict or unavailable offline evidence becomes review/rescan without overwriting either evidence source. The later MapCase02 increment is reviewed separately above and does not alter this ESL evidence contract.

The independent ESL review closed the earlier session/finalization races and two I6 follow-up P1s: callback/evidence timeout could revoke a fresh request B using generation-only restart, and manifest v3 could incorrectly force a legacy tag v1 through verified-burst authority. The current implementation uses one session admission gate with transaction/reservation drain, double generation/finalization checks around prior-map queue work, explicit ordinary-versus-terminal Recovery permission, an active-only audit API bound to the capture generation's exact tracking identity, request-specific revoke intent, and versioned tag authority. Re-review result: `P0=0 / P1=0`. The remaining I6 P2 is a scanner-level injectable hung-worker integration test; Windows portable basename aliases/device names, Debug illegal-transition assertion/audit ordering, scan-stop UI latency and other low-impact UX/performance items are also recorded in [`../ESL_CAPTURE_TODO.md`](../ESL_CAPTURE_TODO.md), not treated as blockers.

Current I6 ESL evidence is: capture-focused Swift host PASS, finalization-focused Swift host PASS, ARFrame-only source contract **1/1 PASS**, Stage-3 **82/82**, localized-output-store **28/28**, and session snapshot **10/10**, for **120/120 PASS**; Python syntax, Swift parse and patch-format checks also pass. iOS finalization enforces localized-tag payload/symbology/exact observation-set binding and positive strictly increasing burst sequence. The simulator build emitted the current app Swift module, then failed in native C++ at missing `Eigen/Core`; therefore Apple clean compile-link remains NOT PASS. I5's complete long-running PriorMap host workflow remains historical **1/1 PASS (943.159 s)**, but I6 did not rerun it to completion and the full `discover` suite is not reported as PASS. LiDAR-device continuity, 30-second performance, lighting/reflection/oblique/multiple-tag matrices and field acceptance remain deferred. The ESL increment does not close J-04 or change the overall **REJECTED / NO-GO / developer smoke only** judgment.

V3 exact-HEAD run [`31276419986`](https://github.com/mcjiansheng/MarketScanner/actions/runs/31276419986) and V4 run [`31276999280`](https://github.com/mcjiansheng/MarketScanner/actions/runs/31276999280) remain historical failed evidence. The current chain has advanced through I11/G11; run `31307753672` proves the complete host/RSS and all non-Apple gates but fails iphoneos cold configure. No failed run may be described as exact-SHA CI PASS, and I11 still requires a fresh exact-HEAD run.

Snapshot and Result publication follow the same recovery-first contract: durable intent binds business/SHA identity and directory dev/inode; payloads are frozen while the root remains `0755`; an exclusive sibling rename is followed through the still-open FD by `fchmod(0555)`, directory/parent `fsync`, destination identity and exact business validation. Snapshot task-root/lock, Result root/lock and Map root/lock are descriptor/path-bound and revalidated before public success. Result quarantine now uses a hidden pending wrapper and a durable canonical v2 diagnostic before moving the only source; source/payload/wrapper identities are bound, startup recovery classifies pending/final/temp/tombstone states, unknown conflicts preserve evidence and fail the whole listing closed, and historical v1 immutable wrappers remain compatible, including a top-level symlink payload without permitting nested symlinks. Map quarantine retains diagnostic v3 and strict legacy-v2 policy. macOS ACLs, BSD flags, extended attributes, malicious same-UID non-cooperative writers and non-local filesystem semantics remain outside qualification.

The shared publication helper and directory enumeration open parent/root directories with `O_RDONLY|O_DIRECTORY`; the app owner therefore needs read, write and search permission on the relevant parent, not only write and search. The qualified runtime also requires no-follow directory FDs, directory `fsync`, `lockf`, and same-volume same-parent `renameatx_np(..., RENAME_EXCL)`. Filesystems without those capabilities are outside the current qualification claim.

P7R4 separates measurement acceptance, hypothesis trust, bounded correction application, Recovery convergence, confidence acceptance, and formal constraint disposition. Intermediate Recovery steps are provisional and keep the UI in `recovering`; timeout frames remain weak. A convergence frame is capped at `usable`, followed by at least three consecutive ordinary Local trusted frames before `stable`. Any rejection resets that post-Recovery counter. Only `stable` may authorize automatic tag confirmation.

The 30-second monotonic deadline is checked before and after matching. A matcher result that crosses the deadline may count as a real attempt but cannot mutate the alignment. Attempt 40 may retain a safe bounded anchor step, but its constraint remains provisional and the outcome remains `timed_out` when residual convergence was not reached. Automatic weak/lost Recovery has a centralized 20-second cooldown; reliable RTAB-Map loop closure may bypass only that cooldown and still cannot reset or duplicate an active episode.

Completion diagnostics are bound to the completed episode and kept separate from current Local hypotheses. `PriorMapScanMatchResult.searchPerformed` is the single source for valid-attempt accounting, with `PriorMapScanMatcher.minimumSearchPointCount = 30`. The production-shared update reducer joins frame disposition, monotonic expiry, attempt exhaustion, correction/constraint acceptance, Recovery action, next confidence phase, and diagnostics; limited/no-depth/nil/undersized/busy/throttled frames consume wall time but not attempts, timeout enters and remains weak through cooldown, and exact cooldown expiry permits a new automatic episode. The localizer's production anchor is exercised through competing A/B Recovery, bounded B convergence, track cleanup, and the next ordinary Local frame. The PC reader rejects any provisional disposition marked formally accepted.

Current RC status: **I11/G11 BOUND / RUN 31307753672 HOST + RSS PASS BUT IPHONEOS COLD CONFIG FAILED AND OVERALL 7/8 / I11 FRESH HOST PREBUILD + IPHONEOS CONFIGURE + MAP STUDIO 109/109 PASS / I11 INDEPENDENT REVIEW P0=0 P1=0 / NEW EXACT-SHA REQUIRED / J-04 BLOCKER OPEN / REJECTED FOR QUALIFIED FIELD TEST**. Tomorrow's queue retains the complete PriorMap `discover` run, Replay/FAR, remaining root-enumeration/special-file diagnostics, Apple/device builds and the device/field matrix. Historical reviews remain under [`history/`](history/); this file and `MOBILE_ONLY_V1R5_CLOSEOUT_REPORT.md` carry the current RC judgment.

P7R5 closes the four findings of the independent P7R4 review without touching P7R2/P7R3/P7R4 contracts. F-01: `PriorMapRecoveryController.finish` reconciles the automatic cooldown against the terminal outcome — convergence and manual reset clear stale cooldown (so a successful reliable-loop Recovery no longer forces subsequent frames weak), timeouts extend it regardless of trigger source, and cancellations follow their explicit reason. F-02: teardowns are no longer fire-and-forget; `cancelRecovery(reason:now:)` returns the terminal completion, and every terminal completion (converged/timed out/manual reset/cancelled) is persisted as `MarketScannerRecoveryLifecycleEvent` v1 in `localization_recovery_events.jsonl` before the localizer is unbound, in strict order (finish, build record, write evidence, confirm, then cleanup); append failures increment the required-write failure counter and make the session processing-ineligible (fail closed). F-03: episode elapsed time exposed on completion frames is bound to the completion finish time through one shared diagnostics reducer, never to a later consuming frame clock. F-04: repeated triggers on an active episode retain a bounded source summary (automatic/reliable-loop counters, last reason/uptime, at most eight trigger records).

P7R6 closes the six findings of the independent P7R5 review without regressing P7R2/P7R3/P7R4/P7R5 contracts. R6-01: capture health carries the exact expected-count recovery watermark (`localizationRecoveryEventCount` plus the last persisted episode ID and finish uptime); the watermark advances exactly once per confirmed durable append and never on failure, finalization validates the exact record count and the last record against it, and a missing file or a rebuilt empty file while the watermark is positive stays a hard blocker. R6-02: the PC input manifest version 2 binds `localization_recovery_events.jsonl` into the canonical bundle SHA; sessions without the watermark keep manifest version 1 marked `recovery_lifecycle_evidence_unbound_legacy`, and the marker stays outside the canonical hash so historical digests remain reproducible. R6-03: the lifecycle schema is validated strictly on device and on the PC reader (outcome whitelist, cancellation-reason conditions, elapsed tolerance, counter ranges, trigger classification reconciliation, bounded v2 `trigger_records`, strictly increasing episodes, non-decreasing finish uptimes). R6-04/R6-05: the Foundation-only `RecoveryLifecyclePersistenceCoordinator` runs teardown-to-disk transactions through peek/ack with structured results (`attemptedEpisodeIds`/`persistedEpisodeIds`/`failedEpisodeId`/`failureReason`/`allPersisted`), idempotence strategy A (identical persisted bytes acknowledge without rewriting; conflicting bytes fail closed), and retryable failures that keep completions queued and auditable. R6-06: the exact implementation SHA is bound in `.github/marketscanner-repair-v2-wave.json`. Executable evidence is provided by the W1-W11 watermark-integrity tests, the I1-I15 teardown-to-finalization transactions in the Swift host (real file system, durable writer, fault injection, serialized-queue and wrong-queue behavior), and the P1-P8 input manifest identity tests; the localized report adds `recovery_summary` and a `recovery_gate` whose blockers enter both review and publish gates. Exact-final-SHA seven-group CI, a clean Apple build, the independent review, and the human Sam re-test remain NOT RUN and must not be reported as PASS; the release judgment stays **NO-GO / NOT PRODUCTION READY**, and `SAM FIELD PASS` / `PRODUCTION READY` remain forbidden until the human, LiDAR real-device, same-route Sam, and on-site control-point tests complete.

P7R6A closes the residual parser-contract split found while reviewing P7R6, without regressing P7R2/P7R3/P7R4/P7R5/P7R6 contracts. A: the persistence coordinator no longer auto-decodes persisted lines with a plain `JSONDecoder`; before any append or acknowledgement it stable-reads the whole evidence snapshot and validates it through the new Foundation-only `RecoveryLifecyclePersistedEvidenceParser` (`RecoveryLifecycleEvidenceParser.swift`) — the exact parser the finalization validator now delegates to (one implementation, two call sites; the duplicated recovery business validation was removed from `SupermarketFinalizationCore`). B: a complete JSON record missing its final newline is rejected by the shared parser (`missingFinalNewline`), so the coordinator can never acknowledge a completion that finalization would later reject; the PC reader enforces the same final-newline contract. C: historical v1 records are genuinely parseable by the coordinator (no fabricated `deadline_uptime`/`maximum_valid_attempts`/`trigger_records`), mixed v1/v2 files are legal, and a v1 record against a pending v2 episode of the same ID is `persisted_episode_version_conflict` (fail closed, never merged or upgraded). D: `attemptedEpisodeIds` reports only episodes the transaction actually entered; pre-transaction failures (missing identity, unreadable or unparsable snapshot) report an empty attempted list and keep every completion queued. New stable failure codes: `existing_evidence_<parser stable code>`, `persisted_episode_version_conflict`, `persisted_episode_order_conflict`, `pending_record_encoding_failed`. Executable evidence: P-A1..P-A20 Swift host tests plus shared Swift/PC fixtures under `tools/PriorMap/tests/fixtures/recovery_lifecycle/` whose categories must match on both readers. Exact-final-SHA seven-group CI on the P7R6A HEAD, the clean Apple build, the independent read-only review, and the human Sam re-test remain NOT RUN and must not be reported as PASS; `READY FOR HUMAN SAM RE-TEST` is not declared this round, and `REAL DEVICE PASS` / `SAM FIELD PASS` / `PRODUCTION READY` remain forbidden until LiDAR real-device, same-route Sam, and on-site control-point tests complete.
