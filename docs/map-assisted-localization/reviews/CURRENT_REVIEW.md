# MarketScanner current code-review entry point

> Document status: **当前有效 / current and authoritative**. Last reconciled: 2026-08-07.
> Current external review input: `MarketScanner_Mobile_Only_V1R5_Second_Independent_Full_Code_Review_NO_GO.md`.
> Current implementation input: `MarketScanner_Mobile_Only_V1_Release_Candidate_Blocker_Closeout_Prompt_V2.md`.
> Historical predecessor input: `MarketScanner_P7R4_Review_P7R5_and_Mobile_Only_Master_Prompt.md` and `MarketScanner_P7R1_Agent_Repair_Prompts.md`.
> Production-readiness frozen baseline: `repair-v2-w2r-safety-closeout@cf1b62c949f3574e1804808537e38c8ff643549c`.
> P7R1 review baseline: `repair-v2-p7r0-independent-baseline@79c86b120fc47c28bd350e86abc9b0354d7e838d`.
> P7R3 cloud base at task start: `repair-v2-p7r3-recovery-episode-closeout@998c175e40562fffd85fe45579a358c485a65b30`.
> P7R5 base branch: `repair-v2-p7r4-recovery-confidence-closeout@e2b1cf4142b5a3bc353909af77158199bbf09e5f` (P7R4 production implementation `981ff4e208f74c8d4dd451d32df88233089e3201`).
> P7R5 governance HEAD used as the P7R6 base: `repair-v2-p7r5-recovery-terminal-closeout@970d03fbf18f8c9274f6dfb7ee0b8a79ee193c3f` (P7R5 implementation SHA `fc5618c84280cb02cfda4a8ead03f71401d4b55c`).
> P7R6 governance HEAD used as the P7R6A base: `repair-v2-p7r6-recovery-evidence-integrity-closeout@4bce394bdfb7f6c7c2373f314814a3e3260356fe` (P7R6 implementation SHA `a315ff6e5c0ca639f12c639aaf81be6738195e2f`).
> P7R6A governance HEAD used as the P7R6B base: `repair-v2-p7r6a-recovery-persisted-parser-closeout@f4524d958913e927a33a7295f45ccf7b3a98d42a` (P7R6A implementation SHA `01a42a40e9671c709c4e0f9e1f85839a48dd4a83`).
> P7R6B governance HEAD used as the P7R6C base: `repair-v2-p7r6b-strict-json-pending-queue-closeout@fd3fb4a84bd8da3770a81d455c2cc428f7479567` (P7R6B implementation SHA `fd3fb4a84bd8da3770a81d455c2cc428f7479567`).
> Current implementation branch: `mobile-only-v1-release-candidate-blocker-closeout`, based on `81b6dbb216e843d363fd0088f673076add78013f`. Governance HEAD `37accce79201482926b9c3ea3247f649e95b375b` bound pre-repair implementation `c78106da196ff960b7e1dd78f20cda544023c6ca` while validation remained unbound. Because the macOS immutable-snapshot and Windows exact-case repairs are newer production changes, this implementation commit restores the descriptor's exact implementation/validation placeholders; a later production-drift-free governance commit must bind the new implementation SHA. Stale V1R5 SHAs must not be reused. Mobile V1 follows Route A: Fast reduced graph, at most one Full existing-graph optimization, then `RESCAN_SESSION`; True sensor Deep remains outside V1.
> Mobile-Only V1 modules: docs/mobile-only/.

The authoritative release judgment remains [Production Readiness Review](PRODUCTION_READINESS_REVIEW.md): **NO-GO / NOT PRODUCTION READY**.

The current RC code and host-regression closeout addresses the second V1R5 review except for the explicitly retained RC-B19/J-04 component-identity blocker. The current constraint schema has no atomic bound node/map identity and manual v3 has no RTAB-Map map ID, so neither may be retroactively described as proving the final snapshot graph component. Closing J-04 requires a frozen prior-independent final-link component policy plus a breaking node-snapshot C ABI and constraint/manual evidence-schema migration. The independent read-only review of the pre-CI implementation diff completed and its committed RESCAN/Result recovery, stale task error, strict Bool/reason-disposition/EEXIST, Map quarantine crash/canonicalization, trace `Int64`, and JSONL/RSS findings entered implementation `c78106d`. Local evidence includes PriorMap 166/166, Qualification 28/28, Map Studio 106/106, native 7,878 checks with zero failures, an 82-source local Swift membership inventory, generated-contract drift checks, a 300,000-record finalization run at 12,795,904-byte peak RSS, a 1,728,000-record trace transition storm, and a full 200,000-frame + 200,000-observation tag pipeline at 670,662,656-byte peak RSS. A new portability-targeted `IOSCoreContractTests` rerun also passed 2/2, including immutable snapshot E2E and the three scale workloads. These are unoptimized macOS host developer results, not target-device qualification. RC-B04 exact-SHA CI has executed twice and failed: run `31174285439@00018f4` completed 3/8 jobs successfully; run `31177319567@37accce` completed 6/8, with a macOS 14 frozen-snapshot publication EACCES and a Windows exact-case membership failure. Both defects are fixed locally and the code paths have received read-only review, but the final staged/cached diff and a new exact-SHA required-gate run remain pending; RC-B09 is reopened until that macOS gate succeeds. RC-B29 Replay/FAR policy freeze, RC-B30 Apple clean builds/Device Lab, and the Sam field re-test remain open external gates; the Apple dependency and compile-link steps in run `31177319567` were skipped after the macOS host E2E failure. The permitted status is therefore **REJECTED / NO-GO / developer smoke only**; do not infer `DEVICE LAB TESTABLE`, `DEVICE LAB PASS`, `SAM FIELD PASS`, or `PRODUCTION READY`.

P7R4 separates measurement acceptance, hypothesis trust, bounded correction application, Recovery convergence, confidence acceptance, and formal constraint disposition. Intermediate Recovery steps are provisional and keep the UI in `recovering`; timeout frames remain weak. A convergence frame is capped at `usable`, followed by at least three consecutive ordinary Local trusted frames before `stable`. Any rejection resets that post-Recovery counter. Only `stable` may authorize automatic tag confirmation.

The 30-second monotonic deadline is checked before and after matching. A matcher result that crosses the deadline may count as a real attempt but cannot mutate the alignment. Attempt 40 may retain a safe bounded anchor step, but its constraint remains provisional and the outcome remains `timed_out` when residual convergence was not reached. Automatic weak/lost Recovery has a centralized 20-second cooldown; reliable RTAB-Map loop closure may bypass only that cooldown and still cannot reset or duplicate an active episode.

Completion diagnostics are bound to the completed episode and kept separate from current Local hypotheses. `PriorMapScanMatchResult.searchPerformed` is the single source for valid-attempt accounting, with `PriorMapScanMatcher.minimumSearchPointCount = 30`. The production-shared update reducer joins frame disposition, monotonic expiry, attempt exhaustion, correction/constraint acceptance, Recovery action, next confidence phase, and diagnostics; limited/no-depth/nil/undersized/busy/throttled frames consume wall time but not attempts, timeout enters and remains weak through cooldown, and exact cooldown expiry permits a new automatic episode. The localizer's production anchor is exercised through competing A/B Recovery, bounded B convergence, track cleanup, and the next ordinary Local frame. The PC reader rejects any provisional disposition marked formally accepted.

Current RC status: **PRE-CI IMPLEMENTATION HOST-TESTED / POST-CI LOCAL REPAIR TESTED / EXACT-SHA CI FAILED / REJECTED FOR QUALIFIED FIELD TEST**. The pre-CI review completed, but the post-CI portability repairs still require an exact staged/cached diff review, new implementation binding and an all-green exact-SHA run; J-04 remains a component-identity blocker, and clean simulator/device builds were skipped in the latest run. Replay/FAR, LiDAR Device Lab and Sam field re-test also remain required. Historical reviews remain under [`history/`](history/); `P7R6C_REVIEW.md` is a predecessor record, while this file and `MOBILE_ONLY_V1R5_CLOSEOUT_REPORT.md` carry the current RC judgment.

P7R5 closes the four findings of the independent P7R4 review without touching P7R2/P7R3/P7R4 contracts. F-01: `PriorMapRecoveryController.finish` reconciles the automatic cooldown against the terminal outcome — convergence and manual reset clear stale cooldown (so a successful reliable-loop Recovery no longer forces subsequent frames weak), timeouts extend it regardless of trigger source, and cancellations follow their explicit reason. F-02: teardowns are no longer fire-and-forget; `cancelRecovery(reason:now:)` returns the terminal completion, and every terminal completion (converged/timed out/manual reset/cancelled) is persisted as `MarketScannerRecoveryLifecycleEvent` v1 in `localization_recovery_events.jsonl` before the localizer is unbound, in strict order (finish, build record, write evidence, confirm, then cleanup); append failures increment the required-write failure counter and make the session processing-ineligible (fail closed). F-03: episode elapsed time exposed on completion frames is bound to the completion finish time through one shared diagnostics reducer, never to a later consuming frame clock. F-04: repeated triggers on an active episode retain a bounded source summary (automatic/reliable-loop counters, last reason/uptime, at most eight trigger records).

P7R6 closes the six findings of the independent P7R5 review without regressing P7R2/P7R3/P7R4/P7R5 contracts. R6-01: capture health carries the exact expected-count recovery watermark (`localizationRecoveryEventCount` plus the last persisted episode ID and finish uptime); the watermark advances exactly once per confirmed durable append and never on failure, finalization validates the exact record count and the last record against it, and a missing file or a rebuilt empty file while the watermark is positive stays a hard blocker. R6-02: the PC input manifest version 2 binds `localization_recovery_events.jsonl` into the canonical bundle SHA; sessions without the watermark keep manifest version 1 marked `recovery_lifecycle_evidence_unbound_legacy`, and the marker stays outside the canonical hash so historical digests remain reproducible. R6-03: the lifecycle schema is validated strictly on device and on the PC reader (outcome whitelist, cancellation-reason conditions, elapsed tolerance, counter ranges, trigger classification reconciliation, bounded v2 `trigger_records`, strictly increasing episodes, non-decreasing finish uptimes). R6-04/R6-05: the Foundation-only `RecoveryLifecyclePersistenceCoordinator` runs teardown-to-disk transactions through peek/ack with structured results (`attemptedEpisodeIds`/`persistedEpisodeIds`/`failedEpisodeId`/`failureReason`/`allPersisted`), idempotence strategy A (identical persisted bytes acknowledge without rewriting; conflicting bytes fail closed), and retryable failures that keep completions queued and auditable. R6-06: the exact implementation SHA is bound in `.github/marketscanner-repair-v2-wave.json`. Executable evidence is provided by the W1-W11 watermark-integrity tests, the I1-I15 teardown-to-finalization transactions in the Swift host (real file system, durable writer, fault injection, serialized-queue and wrong-queue behavior), and the P1-P8 input manifest identity tests; the localized report adds `recovery_summary` and a `recovery_gate` whose blockers enter both review and publish gates. Exact-final-SHA seven-group CI, a clean Apple build, the independent review, and the human Sam re-test remain NOT RUN and must not be reported as PASS; the release judgment stays **NO-GO / NOT PRODUCTION READY**, and `SAM FIELD PASS` / `PRODUCTION READY` remain forbidden until the human, LiDAR real-device, same-route Sam, and on-site control-point tests complete.

P7R6A closes the residual parser-contract split found while reviewing P7R6, without regressing P7R2/P7R3/P7R4/P7R5/P7R6 contracts. A: the persistence coordinator no longer auto-decodes persisted lines with a plain `JSONDecoder`; before any append or acknowledgement it stable-reads the whole evidence snapshot and validates it through the new Foundation-only `RecoveryLifecyclePersistedEvidenceParser` (`RecoveryLifecycleEvidenceParser.swift`) — the exact parser the finalization validator now delegates to (one implementation, two call sites; the duplicated recovery business validation was removed from `SupermarketFinalizationCore`). B: a complete JSON record missing its final newline is rejected by the shared parser (`missingFinalNewline`), so the coordinator can never acknowledge a completion that finalization would later reject; the PC reader enforces the same final-newline contract. C: historical v1 records are genuinely parseable by the coordinator (no fabricated `deadline_uptime`/`maximum_valid_attempts`/`trigger_records`), mixed v1/v2 files are legal, and a v1 record against a pending v2 episode of the same ID is `persisted_episode_version_conflict` (fail closed, never merged or upgraded). D: `attemptedEpisodeIds` reports only episodes the transaction actually entered; pre-transaction failures (missing identity, unreadable or unparsable snapshot) report an empty attempted list and keep every completion queued. New stable failure codes: `existing_evidence_<parser stable code>`, `persisted_episode_version_conflict`, `persisted_episode_order_conflict`, `pending_record_encoding_failed`. Executable evidence: P-A1..P-A20 Swift host tests plus shared Swift/PC fixtures under `tools/PriorMap/tests/fixtures/recovery_lifecycle/` whose categories must match on both readers. Exact-final-SHA seven-group CI on the P7R6A HEAD, the clean Apple build, the independent read-only review, and the human Sam re-test remain NOT RUN and must not be reported as PASS; `READY FOR HUMAN SAM RE-TEST` is not declared this round, and `REAL DEVICE PASS` / `SAM FIELD PASS` / `PRODUCTION READY` remain forbidden until LiDAR real-device, same-route Sam, and on-site control-point tests complete.
